import { Box, Container } from "@mui/material";

function AdminBar() {
  return (
    <Container sx={{ minWidth: "100vw", margin:0, marginBottom: "2rem" , bgcolor:"turquoise"}}>
      <Box sx={{ width: "100%", padding:"0.3rem" }}>ADMINBAR</Box>
    </Container>
  );
}

export default AdminBar;
